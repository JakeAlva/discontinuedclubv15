# Use a restricted test key while building. Never commit a real key.
STRIPE_SECRET_KEY=rk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Enable only after Stripe Tax is configured for the registrations you hold.
STRIPE_AUTOMATIC_TAX=false
# Set this only after deliberately reviewing where the store must collect tax.
STRIPE_TAX_REVIEWED=false

PUBLIC_SITE_URL=https://discontinuedclub.com
STRIPE_DEFAULT_TAX_CODE=
